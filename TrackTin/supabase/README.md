# Supabase

Ejecuta `tracktin_schema.sql` en el SQL Editor. Después despliega la función con `supabase functions deploy tracktin-tmdb` y guarda el token con `supabase secrets set TMDB_API_TOKEN=...`. Configura en Auth las URLs de redirección del sitio de GitHub Pages.
